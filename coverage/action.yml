name: "ACE ant coverage actions"
description: "ACE ant coverage actions"
author: "richard@bettercodingtools.com"

inputs:
  TRACE_ANT_FILE_NAME:
    description: "ant file to run"
    required: true
    default: "trace_build.xml"
  

runs:
  using: "docker"
  image: "Dockerfile"

branding:
  icon: "mic"
  color: "purple"